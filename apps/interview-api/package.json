{
  "name": "@portfolio/interview-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "dist/server.js",
  "types": "dist/server.d.ts",
  "scripts": {
    "dev": "npm run build --workspace @portfolio/interview-core && node --import tsx src/server.ts",
    "build": "npm run build --workspace @portfolio/interview-core && tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "npm run build --workspace @portfolio/interview-core && node --import tsx --test src/*.test.ts src/services/*.test.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@portfolio/interview-core": "*",
    "fastify": "^5.3.3",
    "openai": "^6.0.1",
    "zod": "^4.0.0"
  }
}
